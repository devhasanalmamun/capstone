# Base URL of the backend API.
# Local dev is set in .env.local (git-ignored). For production, set this in the
# Render Static Site dashboard (Environment) so it is baked in at build time:
#   VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=http://127.0.0.1:9092
